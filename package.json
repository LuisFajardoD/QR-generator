{
  "name": "qr-ci-jest",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "coverage": "jest --coverage --coverageReporters=text-summary --coverageReporters=lcov"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.1.2",
    "eslint": "^9.6.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
